# Generated by desktop/scripts/prepare-runtime.py. Do not edit.
services:
  postgresql:
    image: qraft-desktop/postgresql:2.2.0
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-qraft}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Run python3 scripts/configure.py first}
      POSTGRES_DB: ${POSTGRES_DB:-qraft}
    volumes:
    - pgdata:/var/lib/postgresql/data
    - ./deploy/initdb:/docker-entrypoint-initdb.d
    healthcheck:
      test:
      - CMD-SHELL
      - pg_isready -U ${POSTGRES_USER:-qraft} -d ${POSTGRES_DB:-qraft}
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
    - qraft
    pull_policy: never
    labels:
      io.qraft.desktop.owner: ${QRAFT_DESKTOP_OWNER:?client owner is required}
  migrations:
    restart: 'no'
    command:
    - up
    depends_on:
      postgresql:
        condition: service_healthy
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER:-qraft}:${POSTGRES_PASSWORD:?Run python3
        scripts/configure.py first}@postgresql:5432/${POSTGRES_DB:-qraft}?sslmode=disable
    networks:
    - qraft
    image: qraft-desktop/migrations:2.2.0
    pull_policy: never
    labels:
      io.qraft.desktop.owner: ${QRAFT_DESKTOP_OWNER:?client owner is required}
  temporal-db:
    image: qraft-desktop/temporal-db:2.2.0
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${TEMPORAL_DB_USER:-temporal}
      POSTGRES_PASSWORD: ${TEMPORAL_DB_PASSWORD:?Run python3 scripts/configure.py
        first}
      POSTGRES_DB: ${TEMPORAL_DB_NAME:-temporal}
    volumes:
    - temporal_pgdata:/var/lib/postgresql/data
    healthcheck:
      test:
      - CMD-SHELL
      - pg_isready -U ${TEMPORAL_DB_USER:-temporal} -d ${TEMPORAL_DB_NAME:-temporal}
      interval: 5s
      timeout: 5s
      retries: 10
    networks:
    - qraft
    pull_policy: never
    labels:
      io.qraft.desktop.owner: ${QRAFT_DESKTOP_OWNER:?client owner is required}
  redis:
    image: qraft-desktop/redis:2.2.0
    restart: unless-stopped
    command: 'redis-server --maxmemory 256mb --maxmemory-policy allkeys-lru --appendonly
      yes

      '
    volumes:
    - redisdata:/data
    healthcheck:
      test:
      - CMD
      - redis-cli
      - ping
      interval: 5s
      timeout: 3s
      retries: 10
    networks:
    - qraft
    pull_policy: never
    labels:
      io.qraft.desktop.owner: ${QRAFT_DESKTOP_OWNER:?client owner is required}
  minio:
    image: qraft-desktop/minio:2.2.0
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY:?Run python3 scripts/configure.py first}
    volumes:
    - miniodata:/data
    healthcheck:
      test:
      - CMD
      - mc
      - ready
      - local
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
    - qraft
    pull_policy: never
    labels:
      io.qraft.desktop.owner: ${QRAFT_DESKTOP_OWNER:?client owner is required}
  temporal:
    image: qraft-desktop/temporal:2.2.0
    restart: unless-stopped
    depends_on:
      temporal-db:
        condition: service_healthy
    environment:
      DB: postgres12
      DB_PORT: 5432
      POSTGRES_USER: ${TEMPORAL_DB_USER:-temporal}
      POSTGRES_PWD: ${TEMPORAL_DB_PASSWORD:?Run python3 scripts/configure.py first}
      POSTGRES_SEEDS: temporal-db
      TEMPORAL_ADDRESS: temporal:7233
      TEMPORAL_CLI_ADDRESS: temporal:7233
    healthcheck:
      test:
      - CMD
      - temporal
      - workflow
      - list
      - --namespace
      - default
      interval: 10s
      timeout: 5s
      retries: 20
      start_period: 30s
    networks:
    - qraft
    pull_policy: never
    labels:
      io.qraft.desktop.owner: ${QRAFT_DESKTOP_OWNER:?client owner is required}
  api:
    restart: unless-stopped
    command:
    - ./server
    - serve
    depends_on:
      migrations:
        condition: service_completed_successfully
      postgresql:
        condition: service_healthy
      redis:
        condition: service_healthy
      temporal:
        condition: service_healthy
      minio:
        condition: service_healthy
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER:-qraft}:${POSTGRES_PASSWORD:?Run python3
        scripts/configure.py first}@postgresql:5432/${POSTGRES_DB:-qraft}?sslmode=disable
      POSTGRES_HOST: postgresql
      POSTGRES_USER: ${POSTGRES_USER:-qraft}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Run python3 scripts/configure.py first}
      POSTGRES_DB: ${POSTGRES_DB:-qraft}
      REDIS_URL: redis://redis:6379/0
      REDIS_ADDR: redis:6379
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:?Run python3 scripts/configure.py first}
      MINIO_BUCKET: ${MINIO_BUCKET:-qraft}
      MINIO_USE_SSL: 'false'
      TEMPORAL_ADDRESS: temporal:7233
      TEMPORAL_NAMESPACE: ${TEMPORAL_NAMESPACE:-default}
      PROVIDER_EFFECT_LEASE: ${PROVIDER_EFFECT_LEASE:-45m}
      ANTHROPIC_API_KEY: ''
      ANTHROPIC_BASE_URL: ''
      ANTHROPIC_MODEL: ''
      ANTHROPIC_PROVIDER: ''
      ANTHROPIC_REASONING_EFFORT: ''
      ALGOFORGE_LLM_REQUEST_TIMEOUT: ${ALGOFORGE_LLM_REQUEST_TIMEOUT:-30m}
      ALGOFORGE_EMBEDDING_API_KEY: ''
      ALGOFORGE_EMBEDDING_BASE_URL: ${ALGOFORGE_EMBEDDING_BASE_URL:-}
      ALGOFORGE_EMBEDDING_MODEL: ${ALGOFORGE_EMBEDDING_MODEL:-}
      ALGOFORGE_EMBEDDING_DIMENSIONS: ${ALGOFORGE_EMBEDDING_DIMENSIONS:-1536}
      ALGOFORGE_EMBEDDING_TIMEOUT_SEC: ${ALGOFORGE_EMBEDDING_TIMEOUT_SEC:-30}
      ALGOFORGE_EMBEDDING_ENABLED: ${ALGOFORGE_EMBEDDING_ENABLED:-false}
      ALGOFORGE_EMBEDDING_UI_ALLOW_PUBLIC: 'true'
      ALGOFORGE_EMBEDDING_EXPECTED_STATEMENT_MODEL_VERSION_ID: ${ALGOFORGE_EMBEDDING_EXPECTED_STATEMENT_MODEL_VERSION_ID:-}
      JWT_SECRET: ${JWT_SECRET:?Run python3 scripts/configure.py first}
      ALGOFORGE_SETTINGS_ENCRYPTION_KEY: ${ALGOFORGE_SETTINGS_ENCRYPTION_KEY:?Run
        python3 scripts/configure.py first}
      ALGOFORGE_QG14_MODEL_ROUTING_ENABLED: 'true'
      ALGOFORGE_CUSTOM_GENERATION_API_MODE: ${ALGOFORGE_CUSTOM_GENERATION_API_MODE-jobs-v1}
      ALGOFORGE_QG15_EXPORT_MODE: ${ALGOFORGE_QG15_EXPORT_MODE-qg15-v1}
      ALGOFORGE_QUALITY_MODE: ${ALGOFORGE_QUALITY_MODE-quality-v1}
      ALGOFORGE_S5_DIVERSITY_MODE: ${ALGOFORGE_S5_DIVERSITY_MODE-diversity-v1}
      SANDBOX_URL: http://sandbox:8090
      APP_ENV: development
      APP_DEV_MODE: 'true'
      LOG_LEVEL: ${LOG_LEVEL:-info}
      SERVER_PORT: '8080'
    healthcheck:
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - -O
      - /dev/null
      - http://localhost:8080/health
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
    - qraft
    image: qraft-desktop/backend:2.2.0
    pull_policy: never
    labels:
      io.qraft.desktop.owner: ${QRAFT_DESKTOP_OWNER:?client owner is required}
  worker:
    restart: unless-stopped
    stop_grace_period: 40m
    command:
    - ./worker
    depends_on:
      migrations:
        condition: service_completed_successfully
      postgresql:
        condition: service_healthy
      redis:
        condition: service_healthy
      temporal:
        condition: service_healthy
      minio:
        condition: service_healthy
      sandbox:
        condition: service_healthy
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER:-qraft}:${POSTGRES_PASSWORD:?Run python3
        scripts/configure.py first}@postgresql:5432/${POSTGRES_DB:-qraft}?sslmode=disable
      POSTGRES_HOST: postgresql
      POSTGRES_USER: ${POSTGRES_USER:-qraft}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Run python3 scripts/configure.py first}
      POSTGRES_DB: ${POSTGRES_DB:-qraft}
      REDIS_URL: redis://redis:6379/0
      REDIS_ADDR: redis:6379
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: ${MINIO_ACCESS_KEY:-minioadmin}
      MINIO_SECRET_KEY: ${MINIO_SECRET_KEY:?Run python3 scripts/configure.py first}
      MINIO_BUCKET: ${MINIO_BUCKET:-qraft}
      MINIO_USE_SSL: 'false'
      TEMPORAL_ADDRESS: temporal:7233
      TEMPORAL_NAMESPACE: ${TEMPORAL_NAMESPACE:-default}
      PROVIDER_EFFECT_LEASE: ${PROVIDER_EFFECT_LEASE:-45m}
      ANTHROPIC_API_KEY: ''
      ANTHROPIC_BASE_URL: ''
      ANTHROPIC_MODEL: ''
      ANTHROPIC_PROVIDER: ''
      ANTHROPIC_REASONING_EFFORT: ''
      ALGOFORGE_LLM_REQUEST_TIMEOUT: ${ALGOFORGE_LLM_REQUEST_TIMEOUT:-30m}
      ALGOFORGE_EMBEDDING_API_KEY: ''
      ALGOFORGE_EMBEDDING_BASE_URL: ${ALGOFORGE_EMBEDDING_BASE_URL:-}
      ALGOFORGE_EMBEDDING_MODEL: ${ALGOFORGE_EMBEDDING_MODEL:-}
      ALGOFORGE_EMBEDDING_DIMENSIONS: ${ALGOFORGE_EMBEDDING_DIMENSIONS:-1536}
      ALGOFORGE_EMBEDDING_TIMEOUT_SEC: ${ALGOFORGE_EMBEDDING_TIMEOUT_SEC:-30}
      ALGOFORGE_EMBEDDING_ENABLED: ${ALGOFORGE_EMBEDDING_ENABLED:-false}
      ALGOFORGE_EMBEDDING_UI_ALLOW_PUBLIC: 'true'
      ALGOFORGE_EMBEDDING_EXPECTED_STATEMENT_MODEL_VERSION_ID: ${ALGOFORGE_EMBEDDING_EXPECTED_STATEMENT_MODEL_VERSION_ID:-}
      JWT_SECRET: ${JWT_SECRET:?Run python3 scripts/configure.py first}
      ALGOFORGE_SETTINGS_ENCRYPTION_KEY: ${ALGOFORGE_SETTINGS_ENCRYPTION_KEY:?Run
        python3 scripts/configure.py first}
      ALGOFORGE_QG14_MODEL_ROUTING_ENABLED: 'true'
      ALGOFORGE_CUSTOM_GENERATION_API_MODE: ${ALGOFORGE_CUSTOM_GENERATION_API_MODE-jobs-v1}
      ALGOFORGE_QG15_EXPORT_MODE: ${ALGOFORGE_QG15_EXPORT_MODE-qg15-v1}
      ALGOFORGE_QUALITY_MODE: ${ALGOFORGE_QUALITY_MODE-quality-v1}
      ALGOFORGE_S5_DIVERSITY_MODE: ${ALGOFORGE_S5_DIVERSITY_MODE-diversity-v1}
      SANDBOX_URL: http://sandbox:8090
      APP_ENV: development
      APP_DEV_MODE: 'true'
      LOG_LEVEL: ${LOG_LEVEL:-info}
      WORKER_CONCURRENCY: ${WORKER_CONCURRENCY:-10}
      WORKER_TASK_QUEUE: ${WORKER_TASK_QUEUE:-algoforge-tasks}
      WORKER_BUILD_ID: ${WORKER_BUILD_ID:-}
      WORKER_USE_BUILD_ID_VERSIONING: ${WORKER_USE_BUILD_ID_VERSIONING:-false}
      PROVENANCE_RETENTION_ENABLED: ${PROVENANCE_RETENTION_ENABLED:-true}
      PROVENANCE_RETENTION_INTERVAL: ${PROVENANCE_RETENTION_INTERVAL:-1h}
      PROVENANCE_RETENTION_BATCH_SIZE: ${PROVENANCE_RETENTION_BATCH_SIZE:-1000}
      PROVENANCE_RETENTION_MAX_BATCHES: ${PROVENANCE_RETENTION_MAX_BATCHES:-100}
      OUTBOX_ENABLED: ${OUTBOX_ENABLED:-false}
      OUTBOX_ENDPOINT: ${OUTBOX_ENDPOINT:-}
      OUTBOX_BEARER_TOKEN: ${OUTBOX_BEARER_TOKEN:-}
      OUTBOX_HMAC_SECRET: ${OUTBOX_HMAC_SECRET:-}
      OUTBOX_POLL_INTERVAL: ${OUTBOX_POLL_INTERVAL:-2s}
      OUTBOX_BATCH_SIZE: ${OUTBOX_BATCH_SIZE:-25}
      OUTBOX_LEASE_DURATION: ${OUTBOX_LEASE_DURATION:-5m}
      OUTBOX_MAX_ATTEMPTS: ${OUTBOX_MAX_ATTEMPTS:-10}
      OUTBOX_CONNECT_TIMEOUT: ${OUTBOX_CONNECT_TIMEOUT:-3s}
      OUTBOX_REQUEST_TIMEOUT: ${OUTBOX_REQUEST_TIMEOUT:-10s}
      OUTBOX_MAX_REQUEST_BYTES: ${OUTBOX_MAX_REQUEST_BYTES:-1048576}
      OUTBOX_MAX_RESPONSE_BYTES: ${OUTBOX_MAX_RESPONSE_BYTES:-65536}
      OUTBOX_READINESS_MAX_QUEUED: ${OUTBOX_READINESS_MAX_QUEUED:-1000}
      OUTBOX_READINESS_MAX_AGE: ${OUTBOX_READINESS_MAX_AGE:-5m}
      ALGOFORGE_S3_SANDBOX_IMAGE_DIGEST: ${ALGOFORGE_S3_SANDBOX_IMAGE_DIGEST:-}
      ALGOFORGE_S3_SANDBOX_TOOLCHAIN_MANIFEST_DIGEST: ${ALGOFORGE_S3_SANDBOX_TOOLCHAIN_MANIFEST_DIGEST:-}
      ALGOFORGE_S3_SANDBOX_SECCOMP_POLICY_DIGEST: ${ALGOFORGE_S3_SANDBOX_SECCOMP_POLICY_DIGEST:-}
      ALGOFORGE_S3_HIDDEN_RUNNER_URL: ${ALGOFORGE_S3_HIDDEN_RUNNER_URL:-}
      ALGOFORGE_S3_HIDDEN_RUNNER_BEARER_TOKEN: ${ALGOFORGE_S3_HIDDEN_RUNNER_BEARER_TOKEN:-}
      OUTBOX_HEALTH_ADDR: :8092
    deploy:
      replicas: ${WORKER_REPLICAS:-1}
    expose:
    - '8092'
    healthcheck:
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - --spider
      - http://localhost:8092/readyz
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
    - qraft
    - sandbox_internal
    image: qraft-desktop/backend:2.2.0
    pull_policy: never
    labels:
      io.qraft.desktop.owner: ${QRAFT_DESKTOP_OWNER:?client owner is required}
  frontend:
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
    environment:
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL-}
      NEXT_PUBLIC_WS_URL: ${NEXT_PUBLIC_WS_URL-}
    healthcheck:
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - -O
      - /dev/null
      - http://127.0.0.1:3000
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
    - qraft
    image: qraft-desktop/frontend:2.2.0
    pull_policy: never
    labels:
      io.qraft.desktop.owner: ${QRAFT_DESKTOP_OWNER:?client owner is required}
  sandbox:
    image: qraft-desktop/sandbox:2.2.0
    restart: unless-stopped
    privileged: true
    environment:
      SANDBOX_PORT: '8090'
      SANDBOX_MAX_CONCURRENT: ${SANDBOX_MAX_CONCURRENT:-5}
      SANDBOX_TIME_LIMIT: ${SANDBOX_TIME_LIMIT:-10}
      SANDBOX_MEMORY_LIMIT: ${SANDBOX_MEMORY_LIMIT:-512}
      SANDBOX_IMAGE_DIGEST: ${SANDBOX_IMAGE_DIGEST:-unverified}
      SANDBOX_AUDIT_LOG: /sandbox/audit/audit.jsonl
    volumes:
    - sandbox_audit:/sandbox/audit
    healthcheck:
      test:
      - CMD
      - wget
      - --no-verbose
      - --tries=1
      - -O
      - /dev/null
      - http://localhost:8090/healthz
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
    - sandbox_internal
    pull_policy: never
    labels:
      io.qraft.desktop.owner: ${QRAFT_DESKTOP_OWNER:?client owner is required}
  caddy:
    environment:
      DOMAIN: :80
    image: qraft-desktop/caddy:2.2.0
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
      frontend:
        condition: service_healthy
    volumes:
    - ./deploy/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
    - caddy_data:/data
    - caddy_config:/config
    networks:
    - qraft
    pull_policy: never
    labels:
      io.qraft.desktop.owner: ${QRAFT_DESKTOP_OWNER:?client owner is required}
    ports:
    - 127.0.0.1:${LOCAL_PORT:-18180}:80
    healthcheck:
      test:
      - CMD
      - wget
      - -q
      - --spider
      - http://127.0.0.1/health
      interval: 3s
      timeout: 3s
      retries: 20
volumes:
  pgdata:
    driver: local
    labels:
      io.qraft.desktop.owner: ${QRAFT_DESKTOP_OWNER:?client owner is required}
  temporal_pgdata:
    driver: local
    labels:
      io.qraft.desktop.owner: ${QRAFT_DESKTOP_OWNER:?client owner is required}
  redisdata:
    driver: local
    labels:
      io.qraft.desktop.owner: ${QRAFT_DESKTOP_OWNER:?client owner is required}
  miniodata:
    driver: local
    labels:
      io.qraft.desktop.owner: ${QRAFT_DESKTOP_OWNER:?client owner is required}
  sandbox_audit:
    driver: local
    labels:
      io.qraft.desktop.owner: ${QRAFT_DESKTOP_OWNER:?client owner is required}
  caddy_data:
    driver: local
    labels:
      io.qraft.desktop.owner: ${QRAFT_DESKTOP_OWNER:?client owner is required}
  caddy_config:
    driver: local
    labels:
      io.qraft.desktop.owner: ${QRAFT_DESKTOP_OWNER:?client owner is required}
networks:
  qraft:
    driver: bridge
    labels:
      io.qraft.desktop.owner: ${QRAFT_DESKTOP_OWNER:?client owner is required}
  sandbox_internal:
    driver: bridge
    internal: true
    labels:
      io.qraft.desktop.owner: ${QRAFT_DESKTOP_OWNER:?client owner is required}
